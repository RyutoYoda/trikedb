"""Benchmark helpers used by the repository's regression tests.

The benchmark directory is intentionally not included in the runtime
package, but it is importable from a checkout so tests can exercise the
evaluation helpers directly.
"""
