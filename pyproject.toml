[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trikedb"
version = "0.15.0"
description = "The single-file graph database: a knowledge graph that lives in one YAML file, queried with full SPARQL 1.1. Built for LLM agents."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Ryuto Yoda" }]
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0", "rdflib>=7"]
keywords = ["knowledge-graph", "rdf", "triple-store", "yaml", "llm", "agents", "ontology"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Database",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.urls]
Homepage = "https://github.com/RyutoYoda/trikedb"

[project.optional-dependencies]
mcp = ["mcp>=1.2,<2"]
remote = ["fsspec>=2024.2", "s3fs>=2024.2"]
shacl = ["pyshacl>=0.25"]
owl = ["owlrl>=6.0.2"]
semantic = ["model2vec>=0.3", "numpy>=1.24"]
serve = ["mcp>=1.2,<2", "uvicorn>=0.27", "starlette>=0.37"]
all = [
    "mcp>=1.2,<2", "uvicorn>=0.27", "starlette>=0.37",
    "fsspec>=2024.2", "s3fs>=2024.2",
    "pyshacl>=0.25", "owlrl>=6.0.2",
    "model2vec>=0.3", "numpy>=1.24",
]
dev = [
    "pytest>=7", "mcp>=1.2,<2", "fsspec>=2024.2", "pyshacl>=0.25",
    "owlrl>=6.0.2", "uvicorn>=0.27", "starlette>=0.37", "httpx>=0.27",
    "model2vec>=0.3", "numpy>=1.24",
]

[project.scripts]
trikedb = "trikedb.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
