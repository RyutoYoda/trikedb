# freebase demo workspace — a read-only union of the domain graphs
# regenerate the page: trikedb html examples/freebase_workspace.yaml -o docs/workspace.html
graphs:
  film: freebase_film.yaml
  music: freebase_music.yaml
  books: freebase_books.yaml
  people: freebase_people.yaml
  places: freebase_places.yaml
  misc: freebase_misc.yaml
